---
applications:
  .:
    name: TO-coop-housing
    framework:
      name: rails3
      info:
        mem: 256M
        description: Rails Application
        exec: 
    infra: aws
    url: ${name}.${target-base}
    mem: 256M
    instances: 1
    services:
      to-coop-housing:
        type: postgresql
      postgresql-7cc7d:
        type: postgresql
